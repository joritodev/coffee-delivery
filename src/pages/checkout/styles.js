import styled from "styled-components";

export const CheckContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(448px, 640px));
  grid-auto-rows: auto;
  gap: 40px 10px;

`